#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - program
 *
 * Return: always 0
 */

int main(void)
{
	int i;
	for (i = 0 ; i >= 9 ; i++)
	{
		printf("%d",i)#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - program
 *
 * Return: always 0
 */

			int main(void)
		{
			char i;

			for (i = '0' ;  i <= '9' ; i++)
			{
				putchar(i);
			}
			putchar('\n');
			return (0);
		}
	putchar(i + '0');
	}
return (0);
}
